#include <stdio.h>

/**
 * main - print
 *
 * description: print alphabet
 * this program prints all letters except q and e
 */
int main(void)
{
char ch;
for (ch = 'a' ; ch <= 'z' ; ch++)
{
if (c != 'q' && c != 'e')
{
	putchar(c);
}
}
putchar('\n');
return (0);
}
